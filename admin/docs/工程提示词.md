"""
后台管理服务 - 工程提示词

## 项目概述
这是一个基于 FastAPI 的后台管理服务，提供管理后台的 API 接口。

## 技术栈
- FastAPI: Web 框架
- uvicorn: ASGI 服务器
- Pydantic: 数据验证
- Redis: 缓存/会话存储
- MySQL: 主数据库（预留）

## 目录结构
```
admin/
├── app/
│   ├── __init__.py
│   ├── main.py           # 应用入口
│   ├── api/              # API 路由
│   │   ├── __init__.py
│   │   └── routes.py
│   ├── models/           # 数据模型
│   │   ├── __init__.py
│   │   └── schemas.py
│   ├── services/         # 业务逻辑
│   │   ├── __init__.py
│   │   └── ...
│   └── utils/            # 工具函数
│       ├── __init__.py
│       └── ...
├── config/
│   ├── __init__.py
│   └── settings.py       # 配置管理
├── tests/                # 测试目录
│   └── ...
├── .env                  # 环境变量
├── requirements.txt      # 依赖
└── README.md
```

## 开发规范

### 1. API 设计
- RESTful API 设计风格
- 使用 Pydantic 模型进行请求/响应验证
- 统一的错误响应格式

### 2. 代码组织
- 业务逻辑放在 services/ 目录
- API 路由放在 api/ 目录
- 数据模型放在 models/ 目录

### 3. 配置管理
- 使用 .env 文件管理环境变量
- 使用 pydantic-settings 管理配置

### 4. 测试要求
- 单元测试覆盖核心业务逻辑
- API 测试覆盖主要接口
"""
